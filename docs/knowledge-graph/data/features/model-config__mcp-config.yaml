id: model-config:mcp-config
ntype: feature
label: mcp-config
category: model-config
priority: P3
implementations:
  CKIT:
    kind: structured
    source: declared
  CP:
    kind: structured
    source: verified
  DH:
    kind: structured
    source: verified
  HM:
    kind: structured
    source: verified
