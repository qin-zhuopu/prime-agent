id: O:Session.interrupt
ntype: operation
label: interrupt
entity: Session
endpoints:
  CP:
    name: /chat/interrupt
    http: POST
    style: REST
  DH:
    name: session.cancel
    style: RPC
  HM:
    name: session.interrupt
    style: WS-RPC
